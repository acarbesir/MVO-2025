import pandas as pd
import numpy as np
import os

def load_ground_truth_data(gt_file_path):
    """
    This function is no longer needed as ground truth data is not being used.
    It remains as a placeholder and will always return None.
    """
    print("Ground truth data loading is skipped as it's not required for this configuration.")
    return None, None, None, None